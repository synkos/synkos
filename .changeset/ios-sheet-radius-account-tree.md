---
'@synkos/ui': patch
---

- Register the `account_tree` icon in the icon registry so consumers can render git-/repo-style rows without falling back to the `?` placeholder.
- Bump `IOSSheet` top corner radius from 14px to 20px (`$radius-4xl`) to match iOS 26's "Liquid Glass" sheet presentation.
